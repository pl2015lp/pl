Здійсніть тренування юніграм аналізатора на основі частини корпуса, який відповідає першій або другій літері прізвища студента та виконайте аналіз тексту з частини корпуса, яка відповідає першій або другій літері імені студента. Результати поясніть.
#Чому для деяких слів не встановлені теги.

import nltk
from nltk. corpus import brown
science_fiction_tagged_sents = brown.tagged_sents(categories = 'science_fiction')
adventure_sents = brown.sents(categories = 'adventure') [:10]
adventure_tagged_sents = brown.tagged_sents(categories = 'adventure')
unigram_tagger = nltk.UnigramTagger(science_fiction_tagged_sents)
result = unigram_tagger.tag(adventure_sents[0])
mark = unigram_tagger.evaluate(adventure_tagged_sents)
print '          A tagged sentence in the Brown corpus:'
print adventure_tagged_sents[0]
print
print '          The sentence tagged by the program:'
print result
print
print '          Evaluation:', mark
